import { EmbedBuilder } from 'discord.js';

export const musicEmbed = new EmbedBuilder()
    .setColor(0x000000)
    .setThumbnail("attachment://mettatonEX-dance.gif")
    .setTitle("Some title")
    .setDescription("some description")